import { LightningElement } from 'lwc';

/**
 * Deprecated wrapper kept so pages that placed this component before the
 * tabbed console existed keep working. Use Humanitix Setup instead.
 */
export default class HumanitixSyncAdmin extends LightningElement {}
